export class User {
 
    public id: number;
    public name: string;
    public points: string;
   
    constructor(id:number, name:string, points:string) {
      this.id = id;
      this.name = name;
      this.points = status;
    }
  }